from setuptools import setup

setup(
    use_scm_version=True,
    install_requires=[
        "libcst>=0.3.2",
        "pkginfo>=1.5.0.1",
        "imperfect>=0.1.1",
        "moreorless>=0.1.0",
    ],
)
